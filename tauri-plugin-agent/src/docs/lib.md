---
type: Rust Module
title: lib.rs
resource: tauri-plugin-agent/src/lib.rs
docgen:
  crc: 84503e3e
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  score: 100
  issues: judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Цей файл надає реалізацію плагіна агента. Він реєструє команди для журналу запитів (`journal_*`), клієнта ACP та доменного MCP-моста. Для використання плагіна необхідно зареєструвати його в конструкторі застосунку за допомогою `tauri::Builder::default.plugin` та надати дозвіл `agent:default` у файлі можливостей застосунку.

## Поведінка

1. Викликати `init` для ініціалізації плагіна агента.
2. Плагін реєструє команди для журналу запитів (`journal_*`).
3. Плагін реєструє команди для клієнта ACP (`acp_spawn_agent`, `acp_prompt`, `acp_cancel`, `acp_respond_permission`, `acp_config`).
4. Плагін реєструє команди для мостового зв'язку MCP (`acp_register_catalog`, `acp_mcp_tool_result`, `acp_start_mcp_bridge`).
5. Під час налаштування застосунку плагін створює та керує станом ACP (`AcpState`).
6. Під час налаштування застосунку плагін створює та керує станом мосту MCP (`McpBridgeState`).

## Публічний API

init — Ініціалізує плагін агента, налаштовуючи команди для журналювання, клієнтські команди ACP (запуск, запит, скасування зовнішнього кодувального агента та обробка його запитів на дозвіл), а також мост до домену MCP (виставляє каталог інструментів програми для будь-якого запущеного агента ACP).

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
